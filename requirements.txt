# Blender Config Sync - 依赖清单

# GUI 框架（可选，用于桌面应用）
# PyQt6>=6.4.0          # 现代化跨平台 GUI
# tkinter               # Python 内置，无需安装

# 文件压缩与归档
# 无需额外依赖，使用标准库 zipfile

# 数据处理
# 无需额外依赖，使用标准库 json, pathlib, hashlib

# 开发工具（仅开发时需要）
pytest>=7.0.0           # 单元测试框架
pytest-cov>=4.0.0       # 测试覆盖率报告

# 代码质量检查（可选）
# black>=23.0.0         # 代码格式化
# flake8>=6.0.0         # 代码风格检查
# mypy>=1.0.0           # 类型检查

# 文档生成（可选）
# sphinx>=6.0.0         # API 文档生成

# 打包工具（发布时使用）
# pyinstaller>=5.0.0    # 打包为独立可执行文件
